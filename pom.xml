<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!-- If this exact patch isn't resolvable, bump to the latest 3.5.x -->
        <version>3.5.4</version>
        <relativePath/>
    </parent>

    <groupId>com.km</groupId>
    <artifactId>km-langgraph4j-springai</artifactId>
    <version>1.0.0</version>
    <name>KM Session - LangGraph4j + Spring AI</name>
    <description>
        Companion codebase for the "LangGraph4j in Spring AI" Knowledge Meet session.
        Two graphs live here: a minimal "hello world" graph (the exercise starter) and a
        more complete IT-support ticket triage agent (the live demo) that shows state,
        nodes, conditional edges, checkpointing, human-in-the-loop and multi-graph wiring.
    </description>

    <properties>
        <java.version>17</java.version>
        <!-- LTS line of LangGraph4j (1.9.x is the current experimental line) -->
        <langgraph4j.version>1.8.24</langgraph4j.version>
        <!-- Stable GA line of Spring AI. 1.1.x and 2.0.x are also available. -->
        <spring-ai.version>1.0.6</spring-ai.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.bsc.langgraph4j</groupId>
                <artifactId>langgraph4j-bom</artifactId>
                <version>${langgraph4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot web layer: REST endpoints for the ticket-triage demo -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LangGraph4j core: StateGraph, nodes, edges, checkpointing -->
        <dependency>
            <groupId>org.bsc.langgraph4j</groupId>
            <artifactId>langgraph4j-core</artifactId>
        </dependency>

        <!-- LangGraph4j <-> Spring AI glue (ChatModel-aware helpers, AgentExecutor) -->
        <dependency>
            <groupId>org.bsc.langgraph4j</groupId>
            <artifactId>langgraph4j-spring-ai</artifactId>
        </dependency>

        <!--
          LLM backend #1 (default, used in the live demo): Ollama.
          Fully local, no API key, and Docker-friendly - see docker-compose.yml.
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-ollama</artifactId>
        </dependency>

        <!--
          LLM backend #2 (optional): OpenAI.
          Uncomment to swap the demo onto a hosted model - only application.yml
          and an OPENAI_API_KEY env var change; graph code is untouched because
          it only ever depends on the Spring AI ChatModel abstraction.
        -->
        <!--
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>
        -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>km-langgraph4j-springai</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
